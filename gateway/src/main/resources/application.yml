server:
  port: 80

spring:
  application:
    name: gateway
  cloud:
    loadbalancer:
      ribbon:
        enabled: false
    gateway:
      discovery:
        locator:
          enabled: true

management:
  endpoint:
    health:
      probes:
        enabled: true

